language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t rjustin861/fibo-client-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true rjustin861/fibo-client-test npm run test -- --coverage

after_success:
  - docker build -t rjustin861/fibo-client ./client
  - docker build -t rjustin861/fibo-nginx ./nginx
  - docker build -t rjustin861/fibo-server ./server
  - docker build -t rjustin861/fibo-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin registry-1.docker.io
  # Take those images and push them to docker hub
  - docker push rjustin861/fibo-client
  - docker push rjustin861/fibo-nginx
  - docker push rjustin861/fibo-server
  - docker push rjustin861/fibo-worker
